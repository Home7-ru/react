//-------------------------------------------------
//пример объекта
//-------------------------------------------------

const name ='WFM';
const age = 30;

const obj ={
    name: name,
    age: age
};
console.log(obj);

//-------------------------------------------------
//пример выше можно обозначить и так компилятор поймет что name и age откуда брать
//-------------------------------------------------

const name1 ='WFM';
const age1 = 30;

const obj1 ={ name, age };
console.log(obj1);